import { SecondTemplate, SingleLineToast } from '../Toasts';

export const SingleLineError = () => {
	return <SingleLineToast type='error' />;
};

export const SecondTemplateError = () => {
	return <SecondTemplate type='error' />;
};
